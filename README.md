# Transacting-Ether-In-Solidity
Allows the user to send ether and receive a token through solidity. Its fake ether being transacted as this smart contract was deployed on the Javascript VM test network,.
